# Test inputs

Place your own lung-CT `.mha` case(s) here before running `do_test_run.sh`:

- `images/lung-ct/<case>_0000.mha` — one or more cases (the script exercises
  single-case mode with one input and batch mode with several).

No test data is committed to the repository.
